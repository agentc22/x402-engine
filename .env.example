# =============================================================================
# x402 Gateway Configuration
# =============================================================================

# --- Server ---
PORT=3402
NODE_ENV=development

# --- Database ---
# PostgreSQL connection string (required)
DATABASE_URL=postgresql://user:password@localhost:5432/x402_gateway

# --- Payment Addresses ---
# EVM wallet address for Base (USDC) and MegaETH (USDm) payments
PAY_TO_EVM=0xYourEvmWalletAddress

# Solana wallet address for USDC payments
PAY_TO_SOLANA=YourSolanaWalletAddress

# --- Dev Bypass ---
# Header secret to skip payments in development (X-DEV-BYPASS header)
DEV_BYPASS_SECRET=dev-secret-change-me

# --- Facilitator ---
# Official x402 facilitator for Base and Solana payments
FACILITATOR_URL=https://x402.org/facilitator

# --- MegaETH ---
# MegaETH mainnet RPC (default: https://mainnet.megaeth.com/rpc)
MEGAETH_RPC=https://mainnet.megaeth.com/rpc

# USDm stablecoin contract on MegaETH (18 decimals, NOT 6 like USDC)
MEGAETH_USDM_ADDRESS=0xFAfDdbb3FC7688494971a79cc65DCa3EF82079E7

# --- Upstream API Keys ---
# All providers support key pool rotation: comma-separate multiple keys
# for higher throughput. Falls back to singular form if plural not set.
#
# Examples:
#   FAL_API_KEYS=key1,key2,key3    (pool of 3 keys)
#   FAL_API_KEY=key1               (single key, also works)

# === COMPUTE PROVIDERS ===
FAL_API_KEYS=                 # https://fal.ai/dashboard
E2B_API_KEYS=                 # https://e2b.dev
DEEPGRAM_API_KEYS=            # https://deepgram.com

# === CRYPTO DATA ===
COINGECKO_API_KEYS=           # https://www.coingecko.com/en/api (optional, free tier works without key)
ALLIUM_API_KEYS=              # https://allium.so

# === IPFS STORAGE ===
PINATA_JWTS=                  # https://pinata.cloud (JWT tokens)
PINATA_GATEWAY=               # Your dedicated gateway (optional, default: gateway.pinata.cloud)
