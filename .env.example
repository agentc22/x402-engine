# =============================================================================
# x402 Gateway Configuration
# =============================================================================

# --- Server ---
PORT=3402
NODE_ENV=development

# --- Payment Addresses ---
# EVM wallet address for Base (USDC) and MegaETH (USDm) payments
PAY_TO_EVM=0xYourEvmWalletAddress

# Solana wallet address for USDC payments
PAY_TO_SOLANA=YourSolanaWalletAddress

# --- Dev Bypass ---
# Header secret to skip payments in development (X-DEV-BYPASS header)
DEV_BYPASS_SECRET=dev-secret-change-me

# --- Facilitator ---
# Official x402 facilitator for Base and Solana payments
FACILITATOR_URL=https://x402.org/facilitator

# --- MegaETH ---
# MegaETH mainnet RPC (default: https://mainnet.megaeth.com/rpc)
# NOTE: alpha.megaeth.com does NOT resolve — use mainnet.megaeth.com
MEGAETH_RPC=https://mainnet.megaeth.com/rpc

# USDm stablecoin contract on MegaETH (18 decimals, NOT 6 like USDC)
MEGAETH_USDM_ADDRESS=0xFAfDdbb3FC7688494971a79cc65DCa3EF82079E7

# --- Upstream API Keys ---
# Comma-separated for key pool rotation (minimum 1 key per service)
OPENWEATHER_API_KEYS=key1,key2
BRAVE_SEARCH_API_KEYS=key1,key2
GOOGLE_PLACES_API_KEYS=key1,key2

# === COMPUTE PROVIDERS ===
FAL_API_KEY=                  # https://fal.ai/dashboard
E2B_API_KEY=                  # https://e2b.dev
DEEPGRAM_API_KEY=             # https://deepgram.com

# === CRYPTO DATA ===
COINGECKO_API_KEY=            # https://www.coingecko.com/en/api (optional, free tier works without key)

# === BLOCKCHAIN RPC ===
ALCHEMY_API_KEY=              # https://www.alchemy.com
RPC_ETHEREUM=                 # Override default RPC URLs if needed
RPC_BASE=
RPC_ARBITRUM=
RPC_POLYGON=
RPC_OPTIMISM=

# === IPFS STORAGE ===
PINATA_JWT=                   # https://pinata.cloud
PINATA_GATEWAY=               # Your dedicated gateway (optional, default: gateway.pinata.cloud)
